#pragma once

#include "../neededLibs.h"

namespace IDGenerator{
    string uuid();
    // size_t Restaurant(int checker = -1);
    // size_t Menu(int checker = -1, size_t candidateID = 0);
    // size_t Order(int checker = -1, size_t candidateID = 0);
    // size_t MenuItem(int checker = -1, size_t candidateID = 0);
    // size_t Food(string typeOfFood, int checker = -1, size_t candidateID = 0);
    // size_t Customer(int checker = -1, size_t candidateID = 0);
    // size_t Restaurateur(int checker = -1, size_t candidateID = 0);
    // size_t Admin(int checker = -1, size_t candidateID = 0);
    // size_t Containers(string typeOfContainer, int checker = -1, size_t candidateID = 0);
};

// If checker is active/given, the business of the functions are to check wheter is 
// valid or not. Return 1 if is and 0 if isn't